---
title: "2. Classic — the unsigned map"
---

import NeededForThisScreen from '@site/src/components/textbook/NeededForThisScreen';
import TermFirst from '@site/src/components/textbook/TermFirst';
import GexbotConcept from '@site/src/components/textbook/GexbotConcept';
import WhoMustAct from '@site/src/components/textbook/WhoMustAct';
import ScreenOrientation from '@site/src/components/textbook/ScreenOrientation';
import DistinguishingNote from '@site/src/components/textbook/DistinguishingNote';
import ChapterTerms from '@site/src/components/textbook/ChapterTerms';
import CheckYourself from '@site/src/components/textbook/CheckYourself';
import AdjacentConcepts from '@site/src/components/textbook/AdjacentConcepts';
import OfficialFigure from '@site/src/components/textbook/OfficialFigure';
import PayoffSketch from '@site/src/components/charts/PayoffSketch';
import GreeksSurface from '@site/src/components/charts/GreeksSurface';

# Classic — the unsigned map

**Which Gexbot screen this is.** Classic — the unsigned gamma-exposure map from [Chapter 1](/docs/plans/01-what-gexbot-is).

**What you will be able to do.** Read left and right on the histogram, say what last night’s leftover contracts add versus today’s trading, name the tallest pile and the fulcrum, and write the pressure word **unknown** every time.

<TermFirst id="unknown-pressure" />

**What you already need.** Chapter 1’s meanings of GEX, unsigned, open interest, and volume. You still do not need to have traded an option. This chapter teaches the contract words the histogram is drawn from.

## Words the histogram is made of

Classic’s bars are built from a handful of contract words. A reader without them sees a shape where there is a count.

Classic is a **histogram**: a stack of bars, one bar per **strike**. A strike is the price level written on the option contract — the shelf in the warehouse photo.

There are two kinds of option. A **call** is the right to *buy* the index at that strike. A **put** is the right to *sell* the index at that strike. The buyer of either contract pays a **premium** (the ticket price). The seller collects it.

The contract dies at **expiry**. If that expiry is *today*, this book says **0DTE** (zero days to expiry). Those contracts own most of the fast-changing sensitivity you will meet as **gamma** in a moment. That is why 0DTE bars dominate Classic.

Where the strike sits versus the index is **moneyness**. Near the index: **at the money (ATM)**. A call with strike below the index, or a put with strike above it: **in the money (ITM)**. The other way: **out of the money (OTM)**.

Chapter 1 said the firms in the middle of the options market buy and sell the index as it moves. How much index they need to hold against one contract looks like a fixed amount. It is not. A reader who treats it as fixed expects the same futures flow at every print, and is looking at the wrong force once the index walks.

At expiry the call’s payoff is a kink: worthless below the strike, then a straight line up. Before expiry the live price is a smooth curve that leans toward that kink as the clock runs. The first toy on this page is that kink. It is not the live curve.

The slope of that live curve is how stock-like the contract is right now. The schoolbook name is **delta**. A call traces an S from 0 (far out of the money) through about 0.50 at the money to 1 (deep in the money). A put traces the same S from 0 to −1. Lottery stub at one end; the index in costume at the other.

**Gamma** is the slope of that S — how fast delta itself changes per point. Plot it against the index and the picture is a hill. The hill is the same for a call and a put. Gamma has no up or down. Think of it not as the car’s speed but as how sharply the accelerator itself gets more or less sensitive as you press it.

An at-the-money 0DTE call sits near 0.50 delta. After a 15-point rise it sits nearer 0.80. That walk *is* gamma. A call that still has months left has a flatter S, so the same 15 points barely move delta.

The buyer of a call or a put is long gamma: their S steepens in their favor as the index runs. The seller of either is short gamma. A put is not negative gamma. The sign is who paid versus who collected, not call versus put.

The hill peaks at the money and grows sharply into expiry. Far from the index the S is already flat — the contract has decided — so gamma is nearly zero. That is why a cheap 0DTE can be a huge gamma pile while an expensive far-dated ticket is not.

The ticket price is the **debit** — what the buyer paid. Gexbot weights by gamma, not by debit. A reader who ranks strikes by debit will stare at the expensive far-dated ticket and miss the 0DTE mountain Classic is actually drawing.

Those two shapes are not printed on any tape. They come from differentiating a pricing model: delta is how the model price changes if the index ticks; gamma is how that change itself changes. The schoolbook model is **Black–Scholes** — a call or put priced from the index, the strike, time left, and a single volatility the model is fed. Gexbot’s model is **not stated**. The toys on this page are Black–Scholes. They are not Gexbot.

The listed tape announces the option’s **price**. It does not announce delta or gamma. Each professional book recalculates both, continuously, in its own risk system, whenever the index, the clock, or the vol input moves. Each vendor does the same job independently. Two shops can draw different hills from the same open interest. A Classic bar is one shop’s picture, compared only with itself.

<PayoffSketch />

<GreeksSurface defaultMetric="gamma" />

The first toy is the expiry kink. The second is the live slope and the live hill. They are **not Gexbot**.

As the clock runs, an option’s value tends to leak even if the index does not move. The schoolbook name is **theta**, or time decay. Classic does not plot theta. You will need the word in Chapter 4 when customer incentive appears.

U.S. cash **regular trading hours (RTH)** for this book are 09:30 ET–16:00. Classic’s open-interest map is mostly last night’s official snapshot. Its volume map rewrites during the cash session.

The cash index this running example uses is **SPX** (the S&P 500). Chapter 6 will convert SPX onto ES.

<NeededForThisScreen
  title="Needed to read Classic — collapse if you already own these words"
  ids={['call', 'put', 'strike', 'expiry', 'zero-dte', 'premium', 'moneyness', 'atm', 'itm', 'otm', 'delta', 'gamma', 'black-scholes', 'theta', 'open-interest', 'volume', 'cash-rth', 'spx', 'histogram', 'debit']}
/>

<TermFirst id="histogram" />
<TermFirst id="delta" />
<TermFirst id="debit" />

A **node** is a strike where the pile is large enough to point at. A quiet picture can mean leftover cancelled later — or, on Classic, that call and put cancelled each other. It does not always mean “nothing traded.”

<TermFirst id="node" />

## How to face the screen

Before any story about walls, read the axes. Left and right on Classic are **not** the same as left and right on State.

<ScreenOrientation
  screen="Classic GEX"
  axis="Strike (up and down) × net GEX (left and right)"
  right="Call-sided GEX"
  left="Put-sided GEX"
  color="Docs: bars right = call, bars left = put. Green is not bullish."
  unit="Gamma × open interest, or gamma × today’s volume, call netted against put"
  expiry="A 90-day mix, the nearest expiry, or the next expiry — Chapter 6 names those groups"
  clock="Open interest is last night’s snapshot. Volume GEX rewrites in cash RTH."
/>

<OfficialFigure
  src="/img/gexbot/classic.png"
  alt="Gexbot Classic GEX histogram"
  source="gexbot.com/docs"
  teaching="Right = call-sided GEX; left = put-sided GEX. Owner unknown."
  whoMustAct="unknown"
/>

## GEX from last night’s leftover contracts

Take gamma at a strike, multiply by how many contracts are still open from the official snapshot, net calls against puts. That is **GEX by open interest**. It is last night’s topographic map. The hills are still there at 09:31 even if nobody has walked them today.

<GexbotConcept id="gex-by-oi" />
<WhoMustAct screenId="classic-oi" />

<OfficialFigure
  src="/img/gexbot/gex-oi.webp"
  alt="Classic GEX by open interest"
  source="gexbot.com/docs"
  teaching="Overnight inventory location. Not today’s leftover labeled trades."
  whoMustAct="unknown"
/>

## GEX from today’s trading

Same drawing, but multiply gamma by **today’s volume** at the strike. Footprints on last night’s map. Classic uses *this* series for the fulcrum, the tallest piles, and “which strike just redrew.”

<GexbotConcept id="gex-by-volume" />
<WhoMustAct screenId="classic-volume" />

<OfficialFigure
  src="/img/gexbot/gex-vol.webp"
  alt="Classic GEX by volume"
  source="gexbot.com/docs"
  teaching="Where curvature changed hands today — still unsigned."
  whoMustAct="unknown"
/>

<DistinguishingNote id="gex-by-volume" />

## Call versus put at one strike

At each shelf, Classic subtracts put GEX from call GEX. One bar. Right means the call side won the tug of war. Left means the put side won. Two huge equal piles can net toward zero. That is a **balanced node**, not an empty warehouse.

<TermFirst id="balanced-node" />

<GexbotConcept id="call-put-netting" />
<WhoMustAct id="call-put-netting" />

## The fulcrum Classic calls zero gamma

**Zero gamma** is the weighted center of today’s volume-GEX complex. It is not necessarily a traded strike. Spot can sit on it without anyone having that strike as a wall.

A common story says: below this fulcrum the book is more short-gamma (moves get larger); above it, more long-gamma (moves get pinned). That story **assumes** a usual ownership shortcut — the same one most public GEX commentary assumes. Classic cannot check it, and if the owner is the other way round the story runs backwards. If you use the shortcut, write *if*.

<GexbotConcept id="zero-gamma" />
<WhoMustAct id="zero-gamma" />

## Tallest hill, deepest hole

The fulcrum is a center. The two extremes of the same map are a ranking, not a weighted middle.

A reader who treats the tallest bar as a level the index must stall at has already written an owner. The bar does not say who must act there.

The largest right bar on today’s volume-GEX map is **major positive**. The largest left bar is **major negative**. Last night’s open-interest map has its own pair. The two pairs can sit at different strikes; Classic uses the volume pair for the live tallest piles.

The label jumps for two reasons that look the same on the screen. A bigger node prints at another strike — even a one-print overtake of the runner-up — and the ranking moves. Or the index walks and the gamma surface redraws: gamma peaks at the money, so a strike that was quiet can become the tallest hill without anyone adding a contract. The name moved. Nobody had to trade.

People call these magnets or walls. Whether a hill stalls the index or a hole lets it travel depends on who owns the pile. Classic cannot say.

The reader who always leans against the major is treating a ranking as a bounce. A deep left bar can be an acceleration lane.

<GexbotConcept id="major-pos-neg" />
<WhoMustAct id="major-pos-neg" />

<OfficialFigure
  src="/img/gexbot/classic-majors.png"
  alt="Classic historical majors"
  source="gexbot.com/docs"
  teaching="Majors and earlier-today outlines are locations on an unsigned map."
  whoMustAct="unknown"
/>

## Where the map just redrew

**Max-change** strikes are the shelves whose GEX changed the most over the last 1, 5, 15, or 30 minutes. Location of fresh curvature. Not a direction.

**Lookback** dots show the same histogram at earlier clocks today. The slider replays the day. A pile that *built* into you is a different object from a pile you inherited at 09:31. Dots are not a forecast.

<GexbotConcept id="max-change" />
<WhoMustAct id="max-change" />
<GexbotConcept id="lookbacks" />

## Running example (10:05)

A customer trades **500 SPX 0DTE ATM calls**. Classic’s volume-GEX bar at that strike grows **to the right** (call side).

Now the mirror: the customer **sold** the same 500 calls. Classic’s bar still grows **to the right**. **The bar is identical.** Owner unknown. Pressure unknown.

That is why Classic is not State. You now have a location. Chapter 3 adds the sign — who bought and who sold — and with it the direction of the hedge.

## Summary

The running example’s identical bars are why every Who-must-act row on this page says **unknown**. Location survived the buy/sell split. The hedge sentence did not. A reader who writes a hedge from Classic has used a later chapter’s object on a screen that cannot hold it.

## Terms introduced

<ChapterTerms docId="plans/02-classic" />

## Newcomer questions

1. **Is a big green bar bullish?** It is call-sided gamma. On SPX, Gexbot’s own docs often treat that as *sold* calls (*folklore*). Whether it stalls or fuels depends on owner and on whether implied vol is falling or rising. Classic cannot show the owner.
2. **Call pile and put pile at one strike are huge and equal.** Is the warehouse empty? No. That is a **balanced node**. Classic’s one bar nets toward zero; both piles are still there.

<AdjacentConcepts docId="plans/02-classic" />

<CheckYourself
  items={[
    {
      q: 'Huge equal call and put piles at one strike. The bar nets near zero. May you write pressure none?',
      a: 'No. None is leftover cancelled — a later-screen word. Classic’s net is unsigned location, and both piles are still there. Pressure unknown.',
    },
    {
      q: 'May you write “dealers must buy” under a Classic put-side pile?',
      a: 'No. Write “if the usual ownership shortcut holds, dealers would…” That is assumed, not mandate.',
    },
  ]}
/>

## Gate

On-ramp: [Reading your own ticket](/docs/on-ramp/02-classic) — this chapter’s object from the customer seat.

Next: [Classification and the hedge chain](/docs/plans/03-classification). The leftover-and-label machinery that splits the two branches Classic cannot split.
